import Footer from "@/components/Footer";
import CardEvent from "@/modules/events/CardEvent";

const Test = () => {
  return (
    <section className="relative">
      <Footer />
    </section>
  );
};

export default Test;
