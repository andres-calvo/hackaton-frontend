import FormCreateEvents from "@/modules/events/FormCreateEvents";

const CreateEvent = () => {
  return (
    <section className="flex flex-col h-full justify-center items-center">
      <FormCreateEvents />
    </section>
  );
};

export default CreateEvent;
